import browser from 'ember-clinic-management/utils/browser';
import { module, test } from 'qunit';

module('Unit | Utility | browser', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = browser();
    assert.ok(result);
  });
});
